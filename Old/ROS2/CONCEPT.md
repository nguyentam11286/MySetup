## ROS2 CONCEPT

# OVERVIEW
Combining workspaces using the shell environment.
The core of ROS2 workspace is underlay
Subsequent local workspace is overlays
Allows the installation of several ROS 2 distributions & switching between them
This is accomplished by sourcing setup files every time you open a new shell.


